<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Conference Ticket</title>
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      padding: 40px;
      background: #f8f9fa;
      color: #212529;
    }
    .ticket-wrapper {
      max-width: 800px;
      margin: 0 auto;
      background: #ffffff;
      padding: 32px;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }
    .header {
      text-align: center;
      border-bottom: 2px solid #dee2e6;
      padding-bottom: 16px;
      margin-bottom: 24px;
    }
    .header h1 {
      font-size: 24px;
      font-weight: 700;
      margin: 0;
      color: #1c1c1e;
    }
    .header p {
      font-size: 14px;
      margin: 4px 0 0 0;
      color: #6c757d;
    }
    .section-title {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 12px;
      border-bottom: 1px solid #dee2e6;
      padding-bottom: 6px;
      color: #343a40;
    }
    .info-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 16px;
    }
    .label {
      font-weight: 600;
      font-size: 14px;
      margin-bottom: 2px;
      color: #6c757d;
    }
    .info-group {
      display: flex;
      justify-content: space-between;
      margin-bottom: 12px;
    }
    .info-label {
      font-weight: 500;
      color: #495057;
    }
    .info-value {
      font-weight: 500;
      color: #212529;
    }
    .attendee-block {
      background: #f1f3f5;
      border-radius: 8px;
      padding: 6px 16px;
      margin-top: 12px;
    }
    .amounts {
      margin-top: 16px;
      border-top: 1px solid #ced4da;
      padding-top: 12px;
    }
    .amount-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
    }
    .amount-row strong {
      font-weight: 600;
    }
    .footer {
      margin-top: 32px;
      font-size: 13px;
      color: #868e96;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="ticket-wrapper">
    <div class="header">
      <h1>International Conference on Global Health & Climate</h1>
      <p>Ticket Confirmation</p>
    </div>

    <div class="section-title">Transaction Details</div>
    <div class="info-row">
      <div>
        <span class="label">Transaction ID: <%= attendee.transactionID %></span>
      </div>
      <div>
        <span class="label">Date: <%= createdAt %></span>
      </div>
    </div>

    <div class="section-title">Attendee Information</div>
    <div class="attendee-block">
      <div class="info-group">
        <span class="info-label">Name:</span>
        <span class="info-value"><%= attendee.firstName %> <%= attendee.lastName %></span>
      </div>
      <div class="info-group">
        <span class="info-label">Email:</span>
        <span class="info-value"><%= attendee.email %></span>
      </div>
      <div class="info-group">
        <span class="info-label">Ticket Type:</span>
        <span class="info-value"><%= attendee.ticketsType %></span>
      </div>
      <div class="info-group">
        <span class="info-label">Price:</span>
        <span class="info-value">US$ <%= attendee.price %></span>
      </div>
    </div>

    <div class="amounts">
      <div class="amount-row">
        <span>HSB Tax (15%)</span>
        <span>$<%= attendee?.taxDiscountCupon?.tax || 0 %></span>
      </div>
      <div class="amount-row">
        <span>Group Discount</span>
        <span>-$<%= attendee?.taxDiscountCupon?.groupDiscount || 0 %></span>
      </div>

      <% if (attendee?.taxDiscountCupon?.cupon && attendee?.taxDiscountCupon?.groupDiscount == 0) { %>
        <div class="amount-row">
          <span>Coupon Discount</span>
          <span>-$<%= attendee?.taxDiscountCupon?.cuponPrice %></span>
        </div>
      <% } %>

      <% if (attendee?.taxDiscountCupon?.cupon && attendee?.taxDiscountCupon?.groupDiscount > 0) { %>
        <div class="amount-row">
          <span><%= attendee?.taxDiscountCupon?.cuponApplied %></span>
          <span>-$<%= attendee?.taxDiscountCupon?.cuponPrice %></span>
        </div>
      <% } %>

      <div class="amount-row" style="border-top: 1px solid #ced4da; padding-top: 10px;">
        <strong>Total Amount</strong>
        <strong>US$ <%= attendee?.taxDiscountCupon?.total || attendee.price %></strong>
      </div>
    </div>

    <div class="section-title" style="margin-top: 28px;">Purcher Information</div>
    <div>
      <span class="info-label">Name:</span>
      <span class="info-value"><%= attendee?.purcher?.firstName %> <%= attendee?.purcher?.lastName %></span>
    </div>
    <div>
      <span class="info-label">Email:</span>
      <span class="info-value"><%= attendee?.purcher?.email %></span>
    </div>
    <div>
      <span class="info-label">Phone:</span>
      <span class="info-value"><%= attendee?.purcher?.phone || '-' %></span>
    </div>

    <div class="footer">
      © <%= new Date().getFullYear() %> ICGHC. All rights reserved.
    </div>
  </div>
</body>
</html>
